using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() => std::cout << "Hello";

    // Update is called once per frame
    void Update()
    {
        
    }
}
